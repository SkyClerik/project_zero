# План рефакторинга системы инвентаря (логика размещения предметов)

## Цель:
Разделить логику определения свободного места и размещения предметов в инвентаре от их визуального представления в UI Toolkit. Вся логика размещения должна оперировать исключительно логическими координатами сетки (ячейками), а не пиксельными размерами или `worldBound` UI-элементов. Визуализация будет строиться на основе уже определенной логической позиции.

## Ключевые принципы:
1.  **Разделение ответственности:** Модели данных (ItemBaseDefinition) и логика инвентаря (InventoryPageElement, CraftPageElement) не должны зависеть от визуальных свойств UI (пиксели, worldBound, layout).
2.  **Синхронность логики:** Методы проверки вместимости и поиска места должны быть синхронными и работать только с данными сетки.
3.  **Единообразие:** Работать с размерами предметов и инвентаря в терминах ячеек (`Vector2Int`).

## Детальный план действий:

### Шаг 1: Подготовка данных о размерах предметов (ItemBaseDefinition)
**Файл:** `Assets/SkyClerik/DataEditor/Implementations/Entities/Items/ItemBaseDefinition.cs`
**Изменения:**
*   Убедиться, что `ItemDimensions.DefaultWidth` и `ItemDimensions.DefaultHeight` действительно представляют размер предмета в *ячейках* сетки. Если они используются для других целей или имеют другое значение, необходимо добавить новое свойство `Vector2Int GridSize` в `ItemBaseDefinition` или `ItemDimensions`.
    *   **Предположение:** На текущий момент `DefaultWidth` и `DefaultHeight` в `ItemDimensions` *являются* размерами в ячейках. Будем использовать их как `item.GridSize`.

### Шаг 2: Создание структуры данных для размещенных предметов в сетке (ItemGridData)
**Файл:** `Assets/SkyClerik/ItemsSystem/Scripts/Auxiliary/ItemGridData.cs` (создать новый файл)
**Изменения:**
*   Создать новую `class ItemGridData`, которая будет хранить информацию о размещенном предмете в логике сетки:
    ```csharp
    using UnityEngine;
    using UnityEngine.DataEditor; // Для ItemBaseDefinition

    namespace SkyClerik.Inventory
    {
        [System.Serializable]
        public class ItemGridData
        {
            public ItemBaseDefinition ItemDefinition;
            public Vector2Int GridPosition; // Позиция верхнего левого угла предмета в ячейках сетки
            public Vector2Int GridSize => new Vector2Int(ItemDefinition.Dimensions.DefaultWidth, ItemDefinition.Dimensions.DefaultHeight);
            // Возможно, потребуется хранить информацию о повороте (CurrentWidth, CurrentHeight)
            // или пересчитывать GridSize при повороте. Пока используем Default.

            public ItemGridData(ItemBaseDefinition item, Vector2Int position)
            {
                ItemDefinition = item;
                GridPosition = position;
            }
        }
    }
    ```

### Шаг 3: Обновление интерфейса IDropTarget
**Файл:** `Assets/SkyClerik/ItemsSystem/Scripts/Auxiliary/IDropTarget.cs`
**Изменения:**
*   Добавить новый метод для синхронной проверки размещения в сетке:
    ```csharp
    namespace SkyClerik.Inventory
    {
        public interface IDropTarget
        {
            // ... существующие методы ...

            /// <summary>
            /// Синхронно проверяет, может ли предмет быть размещен в сетке, и возвращает предложенную позицию.
            /// Работает только с логикой сетки (ячейками), без учета пикселей UI.
            /// </summary>
            /// <param name="item">Предмет для размещения.</param>
            /// <param name="suggestedGridPosition">Предложенная позиция в ячейках, если место найдено.</param>
            /// <returns>True, если предмет может быть размещен; иначе False.</returns>
            bool TryFindPlacement(ItemBaseDefinition item, out Vector2Int suggestedGridPosition);

            // Дополнительный метод для получения ItemGridData по ItemVisual (или напрямую по ItemBaseDefinition)
            // Позволит ItemVisual запросить свои GridPosition и GridSize у владельца инвентаря
            ItemGridData GetItemGridData(ItemBaseDefinition itemDefinition);
            
            // Метод для регистрации/отмены регистрации ItemVisual в сетке
            void RegisterVisual(ItemVisual visual, ItemGridData gridData);
            void UnregisterVisual(ItemVisual visual);
        }
    }
    ```

### Шаг 4: Рефакторинг InventoryPageElement (и CraftPageElement)
**Файлы:**
*   `Assets/SkyClerik/ItemsSystem/Pages/InventoryPageElement.cs`
*   `Assets/SkyClerik/ItemsSystem/Pages/CraftPageElement.cs`

**Общие изменения для обоих классов:**
*   **Удалить `List<ItemVisual> _itemVisuals`:** Заменить его на `Dictionary<ItemBaseDefinition, ItemGridData> _placedItemsGridData` или `List<ItemGridData> _placedItemsGridData`. `Dictionary` предпочтительнее для быстрого доступа.
*   **Добавить `bool[,] _gridOccupancy;`:** Двумерный массив, представляющий занятость каждой ячейки. Инициализировать его в `ConfigureInventoryDimensions()` на основе `_inventoryDimensions`.
*   **Реализовать `TryFindPlacement(ItemBaseDefinition item, out Vector2Int suggestedGridPosition)`:**
    *   Эта логика будет итерировать по `_gridOccupancy` и проверять `item.GridSize` (из `ItemDefinition.Dimensions.DefaultWidth/Height`) на наличие свободных ячеек.
    *   Обновлять `_gridOccupancy` здесь **не нужно**, этот метод только *проверяет*.
    *   Полностью избавиться от `worldBound` и `layout` в этой логике.
    *   Использовать `_inventoryDimensions` (ширина/высота сетки в ячейках).
*   **Метод `Initialize()` и `Configure()`:**
    *   `ConfigureInventoryDimensions()` теперь также будет инициализировать `_gridOccupancy` на основе `_inventoryDimensions`.
    *   `_gridRect` и `_cellSize` будут использоваться *только* для визуализации и позиционирования `ItemVisual` и `Telegraph`.
*   **Метод `LoadInventory()`:**
    *   Будет использовать `TryFindPlacement` для каждого `item` из `_itemContainer.GetItems()`.
    *   Если `TryFindPlacement` успешен, создавать `ItemGridData`, добавлять его в `_placedItemsGridData`, помечать ячейки в `_gridOccupancy` как занятые.
    *   Затем создавать `ItemVisual`, передавая ему `ItemGridData` (или `item` и `gridPosition`).
    *   Вызовы `GetPositionForItem()` будут удалены или переписаны. `AddItemToInventoryGrid(inventoryItemVisual);` останется.
*   **Метод `AddItemToInventoryGrid(VisualElement item)`:** Останется, но `ItemVisual` будет создаваться с уже известными `gridPosition` и `gridSize`, а `ItemVisual` сам будет пересчитывать свои пиксельные координаты.
*   **Метод `GetPositionForItem()`:** Удалить или переписать, чтобы он больше не использовал `worldBound` и `yield return new WaitForEndOfFrame()`. Вместо этого, он должен будет использовать `TryFindPlacement` и конвертировать `gridPosition` в пиксельные координаты. **Лучше удалить его полностью и использовать `TryFindPlacement` напрямую там, где это необходимо.**
*   **Метод `ShowPlacementTarget(ItemVisual draggedItem)`:**
    *   Вместо `IsWithinGridBounds(draggedItem)` и `FindTargetSlot(draggedItem)` будет использовать `TryFindPlacement(draggedItem.ItemDefinition, out gridPosition)`.
    *   На основе результата `TryFindPlacement` определять `ReasonConflict` (`None` или `intersectsObjects` или `beyondTheGridBoundary` если `TryFindPlacement` вернул `false`).
    *   `_telegraph` будет позиционироваться путем преобразования найденной `gridPosition` в пиксели, используя `_cellSize`.
*   **Методы `AddStoredItem(ItemVisual storedItem)` / `RemoveStoredItem(ItemVisual storedItem)`:** Будут работать с `ItemGridData` (`_placedItemsGridData`) и `_gridOccupancy`, а не напрямую с `ItemVisual`.
*   **Методы `PickUp(ItemVisual storedItem)` и `Drop(ItemVisual storedItem, Vector2 position)`:** Будут адаптированы для взаимодействия с `_placedItemsGridData` и `_gridOccupancy`. `PickUp` будет удалять из `_gridOccupancy`, `Drop` будет добавлять.

### Шаг 5: Рефакторинг ItemVisual
**Файл:** `Assets/SkyClerik/ItemsSystem/Inventory/Pages/ItemVisual.cs`
**Изменения:**
*   **Конструктор:**
    *   Изменить конструктор для приема `ItemGridData` (или `ItemBaseDefinition`, `Vector2Int gridPosition`, `Vector2Int gridSize`).
    *   Внутри конструктора или нового метода `SetupVisualPosition(Vector2Int gridPosition, Vector2Int gridSize)` вычислить `style.width`, `style.height`, `style.left`, `style.top` на основе `gridPosition`, `gridSize` и `_cellSize` (полученного от владельца инвентаря).
*   **Метод `SetPosition(Vector2 pos)`:** Этот метод теперь будет принимать пиксельные координаты *относительно родителя* (например, `_inventoryGrid`). Вся логика преобразования из `gridPosition` в `pos` будет происходить *вне* `ItemVisual`.
*   **Метод `SetOwnerInventory(IDropTarget dropTarget)`:** Изменить для использования новой логики.
*   **Вся логика перетаскивания (OnMouseDown, OnMouseMove, OnMouseUp, PickUp, Drop):**
    *   Когда `ItemVisual` "поднимается" (`PickUp`), он должен получить свою `ItemGridData` от родительского `IDropTarget`.
    *   `OnMouseMove` будет просто обновлять `ItemVisual` на позиции мыши (в пикселях).
    *   `OnMouseUp` будет вызывать `_characterPages.HandleItemPlacement(this)`, который в свою очередь вызовет `IDropTarget.TryFindPlacement`.

### Шаг 6: Рефакторинг ItemsPage
**Файл:** `Assets/SkyClerik/ItemsSystem/Pages/ItemsPage.cs`
**Изменения:**
*   **Метод `HandleItemPlacement(ItemVisual draggedItem)`:**
    *   Будет вызывать `_inventoryPage.TryFindPlacement(draggedItem.ItemDefinition, out gridPosition)` и `_craftPage.TryFindPlacement(...)`.
    *   На основе найденной `gridPosition` (если найдена), будет вызывать метод на `IDropTarget` для фактического размещения (добавления в `_placedItemsGridData` и обновления `_gridOccupancy`).
    *   Затем будет обновлять `ItemVisual` с новой позицией.
*   **Синхронизация с UI:** Вся логика, которая раньше использовала `worldBound`, теперь будет использовать логику сетки и преобразовывать ее в пиксели только для визуализации.

## Дополнительные замечания:
*   **Телеграф:** `Telegraph` тоже должен будет позиционироваться не по `worldBound` `draggedItem`, а по `suggestedGridPosition` (из `TryFindPlacement`), преобразованной в пиксели.
*   **Rotation:** Поворот предметов также должен быть частью логики сетки (например, `GridSize` может меняться на `(Y, X)` при повороте), а `ItemVisual` будет это визуализировать.

Этот план большой, но он приведет к гораздо более чистому, надежному и гибкому решению для системы инвентаря.

Я готова начать, когда ты скажешь! 💕
